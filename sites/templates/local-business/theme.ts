/**
 * Local-business visual identity (factory shape; research D5). Theme is the ONLY
 * concern this file owns. Direction: an energetic, approachable orange primary.
 */
import type { ThemeConfig } from '~~/sites/core/schemas'

export const localBusinessTheme: ThemeConfig = {
  colors: { primary: '#ea580c' },
}
