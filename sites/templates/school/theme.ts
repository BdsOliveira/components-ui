/**
 * School visual identity (factory shape; research D5). Theme is the ONLY concern
 * this file owns. Direction: a grounded, growth-minded green primary.
 */
import type { ThemeConfig } from '~~/sites/core/schemas'

export const schoolTheme: ThemeConfig = {
  colors: { primary: '#15803d' },
}
