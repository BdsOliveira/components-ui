/**
 * Restaurant visual identity (factory shape; research D5). Theme is the ONLY
 * concern this file owns. Direction: a warm, appetite-forward red primary.
 */
import type { ThemeConfig } from '~~/sites/core/schemas'

export const restaurantTheme: ThemeConfig = {
  colors: { primary: '#b91c1c' },
}
